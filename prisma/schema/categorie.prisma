model Categories {
    id                   String     @id @default(uuid())
    categoriesName       String
    icon                 String?
    categoriesPostsCount Int        @default(0)
    //
    posts                BlogPost[]
    //
    createdAt            DateTime   @default(now())
    updatedAt            DateTime   @updatedAt

    @@map("categories")
}
